# V9.5.1 导航后退修复

问题：
旧版SPA切换页面时没有更新浏览器历史记录，用户从“今日待跟”点击浏览器后退，会直接离开CRM。

修复：
- 使用 History API 保存每次内部页面切换。
- 首次进入系统时把“概览”放入当前历史记录。
- 监听 `popstate`，浏览器后退/前进时恢复相应CRM页面。
- 不会执行退出登录，也不会清除Supabase会话。

测试路径：
1. 打开概览。
2. 点击“今日待跟”。
3. 点击浏览器后退。
4. 应返回概览，不应退出系统。
